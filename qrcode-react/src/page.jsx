import Scan from "./app";
import {BrowserRouter,Routes,Route} from "react-router-dom"

function Page() {
    return(
        <BrowserRouter>
        <Scan/>
        </BrowserRouter>
    )
}

export default Page;